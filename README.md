This repo demonstrates an issue with Next.js on Vercel where directories nested within the `public` directory that contain the substring "public" in their name fail to get built properly and results in a 404 when deployed. This is not the bbehavior that happens locally.

